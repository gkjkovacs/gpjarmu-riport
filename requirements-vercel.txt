# =============================================================================
# Vercel-only dependency manifest for the gpjarmu-riport web GUI
# =============================================================================
# This is the COMPLETE runtime dependency set as resolved by the local venv
# (pip freeze from /home/ger/projects/gpjarmu-riport-v4/.venv, 2026-07-23).
#
# Vercel's Python builder installs ONLY what is listed here — transitive deps
# are NOT auto-resolved unless explicitly pinned. So we list the full transitive
# closure (langchain-openai -> openai, langgraph -> langgraph-checkpoint, etc.).
#
# The V1 `requirements.txt` (50 lines) is bit-identical and untouched.
# This file is a superset + the FastAPI wrapper deps.
# =============================================================================

# --- Core runtime (V1 explicit) ---
pydantic>=2.13,<3.0
pydantic-settings>=2.14,<3.0
python-dotenv>=1.2,<2.0

# --- LangGraph / LangChain (V1 explicit + transitive) ---
langgraph>=0.2,<0.3
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-openai>=0.2,<0.3
langchain-text-splitters>=0.3,<0.4
langgraph-checkpoint>=2.1,<3.0
langgraph-sdk>=0.1,<0.2
langsmith>=0.10,<0.11

# --- HTTP / scraping (V1 explicit + transitive) ---
requests>=2.32,<3.0
beautifulsoup4>=4.12,<5.0
lxml>=5.0,<6.0
pdfplumber>=0.11,<1.0
pdfminer.six>=20260107
pypdfium2>=5.0,<6.0
Pillow>=10.0,<13.0
httpx>=0.28,<0.29
httpcore>=1.0,<2.0
h11>=0.16,<1.0
anyio>=4.0,<5.0
certifi>=2026.0,<2027.0
charset-normalizer>=3.0,<4.0
idna>=3.0,<4.0
urllib3>=2.0,<3.0
soupsieve>=2.0,<3.0

# --- Templating (V1 explicit + transitive) ---
jinja2>=3.1,<4.0
MarkupSafe>=3.0,<4.0

# --- CLI / output (V1 explicit + transitive) ---
rich>=13.7,<14.0
typer>=0.12,<1.0
click>=8.0,<9.0
Pygments>=2.0,<3.0
markdown-it-py>=3.0,<5.0
mdurl>=0.1,<0.2

# --- Pydantic / serialization (transitive) ---
annotated-doc>=0.0,<1.0
annotated-types>=0.7,<1.0
pydantic_core>=2.46,<3.0
typing-extensions>=4.0,<5.0
typing-inspection>=0.4,<1.0
orjson>=3.10,<4.0
ormsgpack>=1.10,<2.0
jsonpatch>=1.30,<2.0
jsonpointer>=3.0,<4.0

# --- LangChain runtime (transitive) ---
tenacity>=9.0,<10.0
regex>=2026.0,<2027.0
tiktoken>=0.7,<1.0
openai>=1.100,<2.0
jiter>=0.5,<1.0
distro>=1.9,<2.0

# --- Async (transitive: langgraph uses asyncio + greenlet) ---
greenlet>=3.0,<4.0

# --- Crypto / config (transitive) ---
cffi>=2.0,<3.0
cryptography>=40.0,<50.0
pycparser>=3.0,<4.0
pluggy>=1.0,<2.0
packaging>=25.0,<26.0
iniconfig>=2.0,<3.0

# --- Web GUI (NEW: FastAPI wrapper for Vercel) ---
fastapi>=0.110,<1.0
mangum>=0.17,<1.0
python-multipart>=0.0.9,<1.0

# --- Dev / test tools (NOT needed by Vercel, kept in requirements-dev.txt instead) ---
# pytest, pytest-asyncio, pytest-mock, respx, responses — see requirements-dev.txt
